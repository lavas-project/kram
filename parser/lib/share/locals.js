
// import {store} from '../../default/store';
import defaultConf from '../../default';

export const locals = {
    // store: store,
    // logger: console,
    repos: {},
    pullers: new Map(),
    hooks: {},
    plugins: [],
    config: {},
    structs: {},
    default: defaultConf
};
